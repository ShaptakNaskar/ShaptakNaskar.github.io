{
  "name": "sappys-directory",
  "version": "1.0.0",
  "description": "Shaptak Naskar's portfolio website",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "cd frontend && npm install && npm run build && cd .. && nodemon backend/server.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.2.3",
    "express": "^4.22.1",
    "mongoose": "^9.1.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "engines": {
    "node": ">=18.x"
  }
}